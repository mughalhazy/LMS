event_name | issue_description | severity | recommended_fix
---|---|---|---
EmployeeCreated | No ownership issue found. Single producer (`employee-service`), domain-aligned to employee lifecycle, and not duplicated across services. | none | No change required.
EmployeeUpdated | No ownership issue found. Single producer (`employee-service`), domain-aligned to employee lifecycle, and not duplicated across services. | none | No change required.
EmployeeTerminated | No ownership issue found. Single producer (`employee-service`), domain-aligned to employee lifecycle, and not duplicated across services. | none | No change required.
DepartmentCreated | Potential domain naming drift: producer is `org-structure-service`, while other architecture docs use organization-oriented names (`organization_catalog_service`, `Organization Structure Service`). This can create contract ambiguity across teams. | medium | Standardize service/domain naming in event contracts (pick one canonical name and alias old name during migration).
DepartmentManagerChanged | Potential domain naming drift: producer is `org-structure-service`, while other architecture docs use organization-oriented names (`organization_catalog_service`, `Organization Structure Service`). This can create contract ambiguity across teams. | medium | Standardize service/domain naming in event contracts (pick one canonical name and alias old name during migration).
UserProvisioned | Potential producer naming inconsistency: event uses `identity-service`, while service maps also reference `auth_identity_service`/`auth-service`. Ownership appears singular but naming mismatch risks accidental duplicate publishers. | high | Define a canonical identity producer service in the event catalog and enforce it via schema registry/topic ACLs.
AccessRoleAssigned | No ownership issue found. Single producer (`access-control-service`), security-domain aligned, and not duplicated across services. | none | No change required.
LeaveRequested | No ownership issue found. Single producer (`leave-service`), leave-domain aligned, and not duplicated across services. | none | No change required.
LeaveApproved | No ownership issue found. Single producer (`approval-service`), approval-domain aligned, and not duplicated across services. | none | No change required.
LeaveRejected | No ownership issue found. Single producer (`approval-service`), approval-domain aligned, and not duplicated across services. | none | No change required.
WorklogSubmitted | No ownership issue found. Single producer (`time-tracking-service`), time-domain aligned, and not duplicated across services. | none | No change required.
WorklogApproved | No ownership issue found. Single producer (`approval-service`), approval-domain aligned, and not duplicated across services. | none | No change required.
PayrollCalculated | No ownership issue found. Single producer (`payroll-service`), payroll-domain aligned, and not duplicated across services. | none | No change required.
PayrollDisbursed | No ownership issue found. Single producer (`payroll-service`), payroll-domain aligned, and not duplicated across services. | none | No change required.
ExpenseSubmitted | No ownership issue found. Single producer (`expense-service`), expense-domain aligned, and not duplicated across services. | none | No change required.
ExpenseApproved | No ownership issue found. Single producer (`approval-service`), approval-domain aligned, and not duplicated across services. | none | No change required.
ExpenseReimbursed | No ownership issue found. Single producer (`finance-service`), finance-domain aligned, and not duplicated across services. | none | No change required.
TrainingAssigned | Domain granularity risk: producer is broad `learning-service`, while architecture decomposes learning responsibilities into multiple learning services (path, enrollment, progress, certification). This can evolve into multi-producer ambiguity. | medium | Split event ownership by bounded context (e.g., `enrollment-service` for assignment, `progress-tracking-service` for completion) or document `learning-service` as an explicit orchestration producer.
TrainingCompleted | Domain granularity risk: producer is broad `learning-service`, while architecture decomposes learning responsibilities into multiple learning services (path, enrollment, progress, certification). This can evolve into multi-producer ambiguity. | medium | Split event ownership by bounded context (e.g., `progress-tracking-service`) or document `learning-service` as an explicit orchestration producer.
PerformanceReviewCompleted | No ownership issue found. Single producer (`performance-service`), performance-domain aligned, and not duplicated across services. | none | No change required.
