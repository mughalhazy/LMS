operation | inputs | outputs
--- | --- | ---
Tool registration (LTI 1.3 + Advantage) | tenant_id, tool_name, initiate_login_uri, redirect_uris[], jwks_uri or public_key_set, requested_scopes[], deployment_context (course/program/org), default_role_mapping, admin_actor_id | tool_id, client_id, deployment_id, platform_issuer, authorization_endpoint, keyset_url, registration_status (`pending_validation`/`active`), created_at
Registration validation and activation | tool_id, jwks fetch result, redirect URI verification result, scope policy check, tenant security policy, admin approval | validation_report (errors/warnings), activation_decision, activated_scopes[], trust_status, audit_event_id
Launch workflow: OIDC login initiation | iss, login_hint, target_link_uri, lti_message_hint, client_id, deployment_id, nonce | authorization_redirect_url, state, nonce_binding_record, correlation_id
Launch workflow: id_token validation and launch context creation | id_token (JWT), state, nonce, platform_jwks, expected_audience/client_id, expected_issuer, deployment_id | launch_validation_status, launch_context_id, resource_link_id, context_id (course/class), launch_claims_snapshot, policy_decisions (allow/deny + reason)
Launch workflow: session + entitlement provisioning | launch_context_id, user_subject, roles[], context_membership, tool_settings, tenant_policies | lms_session_token, effective_permissions[], learner_or_instructor_view, landing_route, session_expiry
User identity mapping (federated link) | tenant_id, issuer (`iss`), platform_user_sub, lis_person_sourcedid (optional), email (optional), given_name/family_name (optional), roles[], provisioning_mode (JIT/pre-provisioned) | lms_user_id, identity_link_id, match_strategy_used (`sub`/`sourcedid`/`email`), account_state (`linked`/`created`/`conflict`), remediation_action (if conflict)
User identity mapping (role normalization) | lti_roles[], context_type, tenant_role_mapping_rules, default_role_fallback | normalized_lms_roles[], enrollment_actions (enroll/upgrade/revoke), authorization_scope_set, role_mapping_audit_id
Tool service access token issuance (AGS/NRPS) | client_assertion_jwt, grant_type (`client_credentials`), requested_scope, tool_id, deployment_id | access_token, token_type, expires_in, granted_scopes[], service_endpoints (lineitems, scores, memberships)
Grade passback (AGS) | access_token, lineitem_id, user_id (mapped), score_given, score_maximum, grading_progress, activity_progress, timestamp | score_write_status, platform_response_code, idempotency_key, retry_instruction
Membership sync (NRPS) | access_token, context_id, pagination_cursor, role_filter (optional) | membership_page[], next_cursor, sync_checkpoint, import_summary
