issue_type | endpoint | service | description | severity | recommended_fix
--- | --- | --- | --- | --- | ---
Service Responsibility Mismatch | `POST /lessons`, `GET /lessons`, `PATCH /lessons/{lessonId}`, `DELETE /lessons/{lessonId}` | core_rest_api (implicit lesson service) | The Stage 2 service map defines `course_catalog_service` and `content_delivery_service` but no standalone lesson service. A top-level lesson resource introduces unclear ownership and weak boundary alignment for course composition vs content delivery concerns. | medium | Assign lesson ownership explicitly to `course_catalog_service` and nest endpoints under course scope (e.g., `/api/v1/courses/{courseId}/lessons`) or define a formal `lesson_service` in the service map.
Endpoint Overlap Across Services | `POST /lessons` (core REST) vs `POST /courses/{courseId}/lessons` (content API) | core_rest_api and content_api | Two different create-lesson contracts exist for the same capability with different resource shapes, causing routing/contract ambiguity and duplicate write paths. | high | Keep one canonical lesson-write endpoint pattern (prefer course-scoped), deprecate the duplicate endpoint, and document migration/versioning at gateway level.
Resource/Entity Mismatch | `/certificates` | core_rest_api vs certification_service | Service map entities are `Certification`, `Badge`, `IssuanceRecord`, and `ExpirationPolicy`, but API exposes only `certificates` and omits badge resources. This leaves part of the declared domain without API representation. | medium | Align resource model with entities by adding `/api/v1/certifications` and `/api/v1/badges` (or formally narrow the domain model to certificates-only).
Cross-Service Responsibility Overlap | `GET /api/v1/analytics/learners/{learnerId}/progress` | reporting_analytics_service vs progress_tracking_service | Analytics exposes learner progress while progress tracking service is the operational owner of progress snapshots/completion records. Without explicit read-model boundaries, this risks duplicated business logic and inconsistent progress semantics. | medium | Define contract split: progress service owns operational progress endpoints (`/api/v1/progress/*`), analytics service exposes aggregated/historical KPI views only.
Missing Authentication/Authorization Requirements | `/users`, `/courses`, `/enrollments`, `/assessments`, `/certificates`, `/lessons` (core REST catalog) | core_rest_api | Core endpoint catalog does not define required auth mechanism, scopes, or role constraints, despite RBAC policy requirements and tenant-scoped authorization rules in Stage 2. | high | Add per-endpoint authn/authz metadata (token type, required permissions, tenant scope, ownership checks), and map each route to RBAC permissions in the contract spec.
Missing Authorization Detail for Authenticated APIs | `/api/v1/auth/*` and `/api/v1/analytics/*` | auth_service_spec and analytics_api | Auth API defines purposes but not client types/authorization policies (public vs confidential client, step-up requirements). Analytics API defines endpoints but not who can access learner/team/org-level datasets. | medium | Extend contracts with access policies (roles/scopes per endpoint), data-scope rules (self/team/tenant), and error semantics for forbidden cross-scope requests.
Namespace/Versioning Inconsistency (Collision Risk) | `/users`, `/courses`, `/lessons` vs `/api/v1/auth/*`, `/api/v1/analytics/*`, `/api/integrations/*` | core_rest_api, auth_service_spec, analytics_api, integration_api | Mixed versioned and unversioned base paths increase gateway policy complexity and make cross-service endpoint governance prone to accidental overlap during future expansion. | medium | Standardize on `/api/v1/{service-or-domain}/...` for all externally exposed services and register reserved prefixes per service.
