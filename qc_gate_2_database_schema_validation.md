issue_type | table_or_entity | owning_service | description | severity | recommended_fix
--- | --- | --- | --- | --- | ---
No issue (single owner confirmed) | students, student_profiles, student_contacts, student_enrollments, teachers, teacher_profiles, teacher_qualifications, teacher_assignments, courses, course_sections, course_schedules, course_materials, attendance_records, attendance_sessions, attendance_policies, exams, assessment_items, grades, grade_scales, fee_structures, invoices, payments, scholarships, guardians, student_guardian_links, emergency_contacts, notifications, notification_templates, notification_deliveries, user_accounts, roles, permissions, session_tokens | Respective domain services in ownership map | Current ownership map assigns each listed table to exactly one service, with no duplicate table ownership declared. | low | Keep ownership registry as source of truth and add CI checks for duplicate ownership entries.
Relationship boundary risk | student_guardian_links | guardian-service | Link table is owned by guardian-service but references student records owned by student-service, which can create cross-service coupling if direct DB joins/writes are allowed across services. | medium | Enforce access via service APIs/events only; prohibit cross-service DB credentials and direct table joins.
Terminology inconsistency | auth entities (`UserAccount`, `SessionToken`) vs auth entities (`UserCredential`, `Session`) | auth-service / auth_identity_service | Stage 2 artifacts use different canonical names for identity/auth entities across ownership rules and service map, increasing risk of duplicate models/tables and unclear ownership. | high | Standardize on one auth domain vocabulary and publish canonical entity-to-table mapping.
Terminology inconsistency | assessment entities (`Exam`, `AssessmentItem`) vs schema table (`assessments`) | assessment-service | Assessment domain naming differs between ownership rules and core schema (`Exam` vs `Assessment`), which can lead to mismatched contracts and event/schema drift. | medium | Pick one canonical term (`Assessment`) across service specs, events, and table names; alias legacy terms during migration.
Terminology inconsistency | certificate lifecycle (`certificates` table) vs service entity (`Certification`) | certification_service (service map) / core schema | Core relational schema uses `certificates` while service map models `Certification`; this creates ambiguity about whether they represent one lifecycle or separate concepts. | medium | Unify terminology and document explicit mapping (e.g., `certifications` + issuance records) in ownership and schema docs.
No issue (core FK semantics valid) | tenants, organizations, users, courses, lessons, enrollments, assessments, certificates | core LMS transactional domain | Declared core relationships are internally coherent (tenant/org/user/course/enrollment/certificate chains and uniqueness constraints are logically valid). | low | Maintain FK/unique constraints in migrations and add schema-level validation tests.
