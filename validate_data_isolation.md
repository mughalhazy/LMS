service_name | table_or_entity | issue_description | severity | recommended_fix
--- | --- | --- | --- | ---
all-services | students, student_profiles, student_contacts, student_enrollments, teachers, teacher_profiles, teacher_qualifications, teacher_assignments, courses, course_sections, course_schedules, course_materials, attendance_records, attendance_sessions, attendance_policies, exams, assessment_items, grades, grade_scales, fee_structures, invoices, payments, scholarships, guardians, student_guardian_links, emergency_contacts, notifications, notification_templates, notification_deliveries, user_accounts, roles, permissions, session_tokens | Table ownership appears isolated: each table is declared under exactly one owning service in the current ownership map; no duplicate table ownership is documented. | low | Keep a single source-of-truth ownership registry and enforce it via schema governance checks in CI.
guardian-service + student-service | student_guardian_links | Cross-domain link table is owned by guardian-service but references students from student-service; without strict boundaries this pattern often leads to direct cross-service table coupling. | medium | Enforce write access only through guardian-service APIs; expose student identity lookups via student-service API/events; prohibit foreign-service DB credentials.
all-services | cross-service read/write paths | No explicit service-to-service API contract catalog is present for the listed LMS services, so cross-service access paths are not fully auditable from the available artifacts. | high | Publish and version an API contract matrix (producer, consumer, operation, auth scope) for all inter-service reads/writes.
all-services | domain events for LMS services | Available event catalog lists HR/EMS-style services (employee, payroll, leave, etc.) and does not map to the LMS service set, leaving LMS cross-service event isolation unverifiable. | high | Create an LMS-aligned event catalog (topic, producer, consumers, payload owner) and require cross-service state propagation through these events.
