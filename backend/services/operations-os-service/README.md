# operations-os-service

Admin-facing operational intelligence. Proactive pattern detection (BC-OPS-01), three-tier action classification CRITICAL/IMPORTANT/OPTIONAL (BC-OPS-02), and daily action list generation (BC-OPS-03). Read-only — never writes to transactional records. Spec: `docs/specs/operations-os-spec.md` (MS§5.10).
